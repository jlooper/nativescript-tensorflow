/* Include.gradle configuration: http://docs.nativescript.org/plugins/plugins#includegradle-specification */

android {
	productFlavors {
		"nativescript-tensorflow" {
			dimension "nativescript-tensorflow"
		}
	}
}

repositories {
    jcenter()
    flatDir {
      dirs 'libs'
    }
  }

dependencies {
	compile (name:'tensorflow-lite-0.1.1', ext:'aar')
}